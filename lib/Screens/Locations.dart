import 'package:flutterweatherui/main.dart';
import 'package:flutter/material.dart';

class locations extends State<MyApp> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Locations'),
      ),
      body: Container(
        width: double.infinity,
      ),
    );
  }
}
